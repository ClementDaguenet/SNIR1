/* 
 * File:   ex2_3.c
 * Author: cdaguenet
 *
 * Created on 13 octobre 2021, 09:12
 */

#include <stdio.h>
#include <stdlib.h>

/*
 * 
 */
int main(int argc, char** argv) {
    int moutons;
    int dindons;
    int tete;
    int pattes;
    int indice = 0;
    while (pattes != 100) {
        if (moutons = 36 - indice) {
            dindons = 0 + indice;
            pattes = (36 - indice) * 4 + (0 + indice) * 2;
            indice += 1;
        } else {
            printf("Désolé, je ne trouve pas");
        }
    }
    printf("Le fermier possède %d moutons et %d dindons", moutons, dindons);
    return (EXIT_SUCCESS);
}